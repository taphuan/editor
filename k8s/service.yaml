apiVersion: v1
kind: Service
metadata:
  name: editor-service
  labels:
    app: editor
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30005
    protocol: TCP
    name: http
  selector:
    app: editor

